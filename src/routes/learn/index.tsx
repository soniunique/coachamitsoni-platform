import { createFileRoute, Link } from "@tanstack/react-router";
import { ArrowUpRight, BookOpen, CalendarDays, CheckCircle2, Clock3, ExternalLink, Loader2, MessageSquare, Megaphone, Sparkles, Video } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { LearnShell, SectionHeader } from "@/components/learn/LearnShell";
import { supabase } from "@/integrations/supabase/client";

export const Route = createFileRoute("/learn/")({ component: LearnHome });
type Course={id:string;slug:string;title:string;description:string|null;program_id:string}; type Program={id:string;title:string}; type Module={id:string;course_id:string}; type Lesson={id:string;module_id:string}; type Workshop={id:string;title:string;description:string|null;starts_at:string|null;ends_at:string|null;format:string|null;status:string;meeting_url:string|null}; type Announcement={id:string;title:string;body:string;published_at:string}; type CourseProgress=Course&{total:number;done:number;percent:number}; type ProgramProgress=Program&{total:number;done:number;percent:number;courseCount:number};
function LearnHome(){const[courses,setCourses]=useState<Course[]>([]),[programs,setPrograms]=useState<Program[]>([]),[modules,setModules]=useState<Module[]>([]),[lessons,setLessons]=useState<Lesson[]>([]),[completed,setCompleted]=useState<Set<string>>(new Set()),[workshops,setWorkshops]=useState<Workshop[]>([]),[registered,setRegistered]=useState<Set<string>>(new Set()),[announcements,setAnnouncements]=useState<Announcement[]>([]),[loading,setLoading]=useState(true),[dbError,setDbError]=useState("");
useEffect(()=>{async function load(){const{data:{user}}=await supabase.auth.getUser();if(!user){setDbError("Please sign in to continue.");setLoading(false);return;}const[{data:programData,error:programError},{data:courseData,error:courseError},{data:workshopData,error:workshopError},{data:registrationData,error:registrationError},{data:announcementData,error:announcementError}]=await Promise.all([supabase.from("programs").select("id,title").order("sort_order",{ascending:true}),supabase.from("courses").select("id,slug,title,description,program_id").eq("status","published").order("created_at",{ascending:false}),supabase.from("workshops").select("id,title,description,starts_at,ends_at,format,status,meeting_url").eq("status","upcoming").order("starts_at",{ascending:true,nullsFirst:false}).limit(3),supabase.from("workshop_registrations").select("workshop_id,status").eq("user_id",user.id),supabase.from("announcements").select("id,title,body,published_at").order("published_at",{ascending:false}).limit(5)]);if(programError||courseError||workshopError||registrationError||announcementError){setDbError(programError?.message??courseError?.message??workshopError?.message??registrationError?.message??announcementError?.message??"Unable to load your learning data.");setLoading(false);return;}const loadedCourses=(courseData??[]) as Course[];setPrograms((programData??[]) as Program[]);setCourses(loadedCourses);setWorkshops((workshopData??[]) as Workshop[]);setRegistered(new Set((registrationData??[]).filter(r=>r.status!=="cancelled").map(r=>r.workshop_id)));setAnnouncements((announcementData??[]) as Announcement[]);if(!loadedCourses.length){setLoading(false);return;}const{data:moduleData,error:moduleError}=await supabase.from("course_modules").select("id,course_id").in("course_id",loadedCourses.map(c=>c.id));if(moduleError){setDbError(moduleError.message);setLoading(false);return;}const loadedModules=(moduleData??[]) as Module[];setModules(loadedModules);const moduleIds=loadedModules.map(m=>m.id);const{data:lessonData,error:lessonError}=moduleIds.length?await supabase.from("course_lessons").select("id,module_id").in("module_id",moduleIds):{data:[],error:null};if(lessonError){setDbError(lessonError.message);setLoading(false);return;}const loadedLessons=(lessonData??[]) as Lesson[];setLessons(loadedLessons);const{data:progressData,error:progressError}=loadedLessons.length?await supabase.from("lesson_progress").select("lesson_id,completed").eq("user_id",user.id).in("lesson_id",loadedLessons.map(l=>l.id)):{data:[],error:null};if(progressError){setDbError(progressError.message);setLoading(false);return;}setCompleted(new Set((progressData??[]).filter((row:{completed:boolean})=>row.completed).map((row:{lesson_id:string})=>row.lesson_id)));setLoading(false);}void load();},[]);
const progress=useMemo<CourseProgress[]>(()=>courses.map(course=>{const moduleIds=new Set(modules.filter(m=>m.course_id===course.id).map(m=>m.id));const courseLessons=lessons.filter(l=>moduleIds.has(l.module_id));const done=courseLessons.filter(l=>completed.has(l.id)).length;return{...course,total:courseLessons.length,done,percent:courseLessons.length?Math.round(done/courseLessons.length*100):0};}),[courses,modules,lessons,completed]);const programProgress=useMemo<ProgramProgress[]>(()=>programs.map(program=>{const programCourses=progress.filter(course=>course.program_id===program.id),total=programCourses.reduce((s,c)=>s+c.total,0),done=programCourses.reduce((s,c)=>s+c.done,0);return{...program,total,done,courseCount:programCourses.length,percent:total?Math.round(done/total*100):0};}),[programs,progress]);const overall=useMemo(()=>{const total=progress.reduce((s,c)=>s+c.total,0),done=progress.reduce((s,c)=>s+c.done,0);return{total,done,percent:total?Math.round(done/total*100):0};},[progress]);const continueCourse=useMemo(()=>progress.find(c=>c.percent>0&&c.percent<100)??progress.find(c=>c.percent<100)??null,[progress]);return <LearnShell><SectionHeader eyebrow="Student dashboard" title="Welcome to your learning hub" description="Continue your learning journey and track your progress" action={<Link to="/learn/workshops" className="learn-secondary-button">Explore workshops <ArrowUpRight size={15}/></Link>}/>{loading?<div className="learn-card flex items-center gap-3 p-6 text-sm text-slate-400"><Loader2 size={18} className="animate-spin"/>Loading your learning...</div>:dbError?<div className="learn-card p-6 text-sm text-red-300">{dbError}</div>:<><div className="mb-6 grid gap-4 md:grid-cols-3"><div className="learn-card p-5"><div className="text-xs text-slate-500">Programs</div><div className="mt-2 text-2xl font-bold">{programs.length}</div></div><div className="learn-card p-5"><div className="text-xs text-slate-500">Courses</div><div className="mt-2 text-2xl font-bold">{progress.length}</div></div><div className="learn-card p-5"><div className="text-xs text-slate-500">Overall progress</div><div className="mt-2 text-2xl font-bold">{overall.percent}%</div><div className="mt-1 text-xs text-slate-400">{overall.done} of {overall.total} lessons complete</div></div></div>{continueCourse?<div className="learn-card overflow-hidden p-0"><div className="h-1 bg-gradient-to-r from-cyan-400 via-violet-500 to-orange-400"/><div className="grid gap-6 p-6 md:grid-cols-[1fr_auto] md:items-center"><div><div className="text-xs font-semibold uppercase tracking-[.18em] text-cyan-300">Continue learning</div><h2 className="mt-2 text-xl font-bold">{continueCourse.title}</h2><p className="mt-2 text-sm leading-6 text-slate-400">{continueCourse.description||"Continue where you left off."}</p><div className="mt-5 h-2 overflow-hidden rounded-full bg-white/8"><div className="h-full rounded-full bg-gradient-to-r from-cyan-400 to-violet-500" style={{width:`${continueCourse.percent}%`}}/></div><div className="mt-2 text-xs text-slate-500">{continueCourse.percent}% complete · {continueCourse.done} of {continueCourse.total} lessons</div></div><Link to="/learn/courses/$slug" params={{slug:continueCourse.slug}} className="learn-primary-button">{continueCourse.percent>0?"Continue":"Start course"} <ArrowUpRight size={15}/></Link></div></div>:<div className="learn-card p-8 text-center"><CheckCircle2 className="mx-auto text-emerald-300" size={32}/><h2 className="mt-4 text-lg font-bold">All courses completed</h2><p className="mt-2 text-sm text-slate-400">You have completed all lessons currently available to you.</p><Link to="/learn/courses" className="learn-secondary-button mx-auto mt-5">View courses <ArrowUpRight size={15}/></Link></div>}<div className="mt-6 grid gap-5 lg:grid-cols-[1.4fr_.6fr]"><div className="learn-card p-6"><div className="flex items-center justify-between"><div><div className="learn-eyebrow">Your learning</div><h2 className="mt-2 text-xl font-bold">Program progress</h2></div><Link to="/learn/courses" className="text-xs text-cyan-300">View courses</Link></div><div className="mt-5 space-y-4">{programProgress.map(program=><div key={program.id} className="learn-feed-item"><div className="learn-icon-tile"><BookOpen size={17}/></div><div className="min-w-0 flex-1"><div className="font-semibold truncate">{program.title}</div><div className="mt-1 text-xs text-slate-500">{program.courseCount} {program.courseCount===1?"course":"courses"} · {program.done} of {program.total} lessons complete</div><div className="mt-2 h-1.5 overflow-hidden rounded-full bg-white/8"><div className="h-full rounded-full bg-gradient-to-r from-cyan-400 to-violet-500" style={{width:`${program.percent}%`}}/></div></div><div className="text-sm font-semibold text-cyan-300">{program.percent}%</div></div>)}{!programProgress.length&&<p className="text-sm text-slate-500">No programs available yet.</p>}</div></div><div className="learn-card p-6"><div className="learn-eyebrow">Need help?</div><h2 className="mt-2 text-xl font-bold">Talk to us</h2><p className="mt-2 text-sm leading-6 text-slate-400">Questions about your learning?</p><Link to="/learn/messages" className="learn-secondary-button mt-5 w-full"><MessageSquare size={15}/> Open messages</Link></div></div><div className="mt-6 learn-card p-6"><div className="flex items-center justify-between"><div><div className="learn-eyebrow">Updates</div><h2 className="mt-2 text-xl font-bold">Announcements</h2></div></div>{!announcements.length?<p className="mt-5 text-sm text-slate-500">No announcements yet.</p>:<div className="mt-5 space-y-3">{announcements.map(a=><article key={a.id} className="rounded-xl border border-white/8 bg-white/[.03] p-4"><div className="flex gap-3"><div className="learn-icon-tile"><Megaphone size={16}/></div><div><h3 className="font-semibold">{a.title}</h3><p className="mt-1 whitespace-pre-wrap text-sm leading-6 text-slate-400">{a.body}</p><div className="mt-2 text-xs text-slate-600">{new Date(a.published_at).toLocaleString()}</div></div></div></article>)}</div>}</div><div className="mt-6 learn-card p-6"><div className="flex items-center justify-between gap-4"><div><div className="learn-eyebrow">Live learning</div><h2 className="mt-2 text-xl font-bold">Upcoming workshops</h2></div><Link to="/learn/workshops" className="text-xs text-cyan-300">View all</Link></div>{!workshops.length?<p className="mt-5 text-sm text-slate-500">No upcoming workshops.</p>:<div className="mt-5 space-y-3">{workshops.map(w=><div key={w.id} className="rounded-xl border border-white/8 bg-white/[.03] p-4"><div className="flex flex-col gap-4 md:flex-row md:items-center md:justify-between"><div className="min-w-0"><div className="font-semibold truncate">{w.title}</div>{w.description&&<div className="mt-1 text-xs text-slate-500 line-clamp-1">{w.description}</div>}<div className="mt-3 flex flex-wrap gap-2 text-xs text-slate-400">{w.starts_at&&<span className="learn-meta"><CalendarDays size={14}/>{new Date(w.starts_at).toLocaleString()}</span>}{w.ends_at&&<span className="learn-meta"><Clock3 size={14}/>{new Date(w.ends_at).toLocaleTimeString([], {hour:"numeric",minute:"2-digit"})}</span>}{w.format&&<span className="learn-meta"><Video size={14}/>{w.format}</span>}</div></div><div className="flex shrink-0 flex-wrap gap-2">{registered.has(w.id)?<span className="learn-secondary-button">Registered</span>:<Link to="/learn/workshops" className="learn-primary-button">Register <ArrowUpRight size={14}/></Link>}{registered.has(w.id)&&w.meeting_url&&<a href={w.meeting_url} target="_blank" rel="noreferrer" className="learn-primary-button">Join <ExternalLink size={14}/></a>}</div></div></div>)}</div>}</div></>}</LearnShell>}
